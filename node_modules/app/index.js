var React = require( 'react' );
var ready = require( 'domready' );
var Header = require( 'app/components/Header' );

ready( function () {
  React.renderComponent( Header(), document.querySelector( '#main' ) );
} );