/** @jsx React.DOM */

var React = require('react');

module.exports = React.createClass({
  render: function () {
    var output = `this is a ${this.props.headline}`;
    return this.transferPropsTo(
      <h1>
        {output}
      </h1>
    );
  }
});

module.exports.id = 'Header';
